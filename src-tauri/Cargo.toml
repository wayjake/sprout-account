[package]
name = "sprout-account"
version = "1.0.0"
description = "Sprout Account — Household Ledger"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-shell = "2"

[profile.release]
strip = true
lto = true
